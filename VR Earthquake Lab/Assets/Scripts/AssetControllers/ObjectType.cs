using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * Enum class for the specific object types in a scene
 */ 
public enum ObjectType {
    FLOOR, GLASS, DESK, COFFEETABLE, TV, WALL, TABLE, HOSPITALBED
}
